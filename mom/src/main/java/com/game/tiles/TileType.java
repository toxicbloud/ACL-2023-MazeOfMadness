package com.game.tiles;

/**
 * TileType enum.
 * This is the enum for the tile types.
 */
public enum TileType {
    /** Wall type tile. */
    TYPE_WALL,
    /** Water type tile. */
    TYPE_WATER,
    /** Rock type tile. */
    TYPE_ROCK
}
